import React, { useState, useEffect } from 'react';

function Star({rating}) {
  const [score, setScore] = useState(rating);

  return (
    <div>
      <p></p>
    </div>
  );
}
export default Star;
